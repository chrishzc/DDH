# DDH Phase 0 Version Archive

This directory contains historical Phase 0 package structures.

- `1.0.0/` preserves the original 34-file layout, but residual example
  vocabulary was sanitized by an explicit human cleanup directive. It is not a
  byte-exact copy.
- The currently effective package is
  `docs/semantic-specifications/ddh-phase-0/` version `1.1.0`.
- Archived packages are decision evidence, not current behavioral authority.
