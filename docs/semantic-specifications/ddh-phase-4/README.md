# DDH Phase 4 Task Specification Package

This confirmation-ready package defines tool-neutral Verification Asset quality,
admission, currentness, layered selection, and cost-controlled execution.
It is specification work only. No Phase 4 runtime, adapter, runner, catalog, or
test implementation is authorized until the human confirms the exact
`DDH-P4-SPEC-001@1.0.0` identity and closure digest.

`draft/manifest.json` is the package authority root. The listed closure assets
form one immutable confirmation unit; the derived Catalog described by this
package is discovery only and never an authority source.

